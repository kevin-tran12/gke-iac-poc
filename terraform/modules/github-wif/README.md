# github-wif boundary

Inputs: immutable owner/repository IDs, branch, caller/reusable workflow refs, and
protected environments. Outputs: one provider and four phase-specific
service-account emails. Current implementation: bootstrap.
