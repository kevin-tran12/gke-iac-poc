# project-foundation boundary

Inputs: project ID/name, billing account, API allowlist, labels and budget.
Outputs: project ID/number and state-bucket contract. Current implementation:
`terraform/bootstrap`.
