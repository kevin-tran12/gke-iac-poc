# Lab 06: supply-chain security

Record the enforced Binary Authorization policy and a verified application
digest. Attempt an unattested deployment and require admission denial plus audit
log. Deploy the attested digest through Cloud Deploy and require success. Delete
the temporary Deployment; do not whitelist an entire public registry.
