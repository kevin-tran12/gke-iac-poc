# Lab 09: observability

Submit a synthetic job with trace context and follow it from Gateway/API to Pub/Sub
worker and GCS using correlated, bounded identifiers. Verify HTTP RED metrics,
worker duration/failure metrics, trace spans, backlog and rollout panels. Inject a
readiness failure and prove the alert is actionable without payload exposure.
