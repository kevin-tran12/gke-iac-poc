# Lab 05: network isolation

Baseline worker Pub/Sub/GCS access, then install a temporary selected egress deny.
Prove backlog/dependency signals, absence of secret leakage, and safe queued API
status. Remove the policy, verify backlog drains to one result per job, and rerun
the policy contract tests.
