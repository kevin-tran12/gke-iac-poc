# Lab 03: HPA autoscaling

Record CPU requests and HPA baseline. Run the bounded three-minute load job and
observe CPU utilization, desired/current replicas and Ready Pods. Pass when HPA
scales above one without exceeding six and returns to one after stabilization.
This lab does not claim node scaling.
