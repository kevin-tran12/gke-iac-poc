# Lab 12: regional resilience

Apply the HA profile, verify multi-zone node/Pod distribution, cordon and drain the
explicit failure zone, and observe PDB/topology behavior. The API must retain a
Ready endpoint and durable job completion. Uncordon, verify recovery, then destroy
the regional cluster before returning to core.
