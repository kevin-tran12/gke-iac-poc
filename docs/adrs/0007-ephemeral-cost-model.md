# ADR 0007: ephemeral cost model

**Decision:** optimize for broad, short learning sessions rather than an always-on
free endpoint. Costly profiles require approval and TTL. Teardown is tested and
independently inventoried; budgets are only secondary detection.
