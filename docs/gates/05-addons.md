# Gate 05: addons and policy

Wait for cert-manager and OpenTelemetry rollouts. Prove the cert-manager webhook
rejects malformed resources, Gateway solver support is active, restricted Pod
security is enforced, default-deny blocks an unapproved egress fixture, and a test
trace/metric reaches Google Cloud Observability.
