# Gate 08: public edge

After `public-edge` approval, verify nip.io resolution, certificate chain/name and
expiry, HTTP-to-HTTPS redirect, API/echo/hello routes, safe synthetic header flow,
Cloud Armor WAF denial and bounded rate limiting. Destroy immediately if the
certificate or Gateway misses its timeout.
