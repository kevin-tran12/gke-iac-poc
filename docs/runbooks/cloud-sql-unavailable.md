# Runbook: Cloud SQL unavailable

Confirm the data profile, private service connection, private IP, Admin API,
connector egress ports, IAM DB authentication flag, database user and worker
identity. Stop retry storms, preserve Pub/Sub messages, repair the dependency, run
the migration gate, and reconcile job results. Use backup/PITR only with approval.
